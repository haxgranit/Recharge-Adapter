/* eslint-disable jsdoc/valid-types, jsdoc/require-description-complete-sentence */
// noinspection LongLine

// ----- 3rd-Party Package Types -----
/**
 * jQuery
 * @typedef {import("@/core/libs/jquery")} jQuery - A jQuery object.
 */

/**
 * Axios
 * @typedef {import(axios).AxiosRequestConfig} axiosRequest
 * @typedef {import(axios).AxiosResponse} AxiosResponse
 * @typedef {import(axios).AxiosRequestConfig} AxiosConfig
 * @typedef {import(axios).Method} requestMethod
 * @typedef {import(axios).AxiosInstance} AxiosInstance
 * @typedef {import(axios).AxiosError} AxiosError
 */

// ----- Local Types -----
/**
 * bigcommerce-helper.js
 * @typedef {import("@/utils/bigcommerce-helper").BigcommerceCart} BigcommerceCart - A BigCommerce Cart Helper object.
 *
 * url-helper.js
 * @typedef {import("@/utils/url-helper").buildRequest} buildRequest - A URL Request object.
 * @typedef {import("@/utils/url-helper").requestMixin} requestMixin - A Vue Mixin for creating URL requests.
 *
 * logger.js
 * @typedef {import("@/plugins/logger").Logger} Logger - A Adapter Logger object.
 */
